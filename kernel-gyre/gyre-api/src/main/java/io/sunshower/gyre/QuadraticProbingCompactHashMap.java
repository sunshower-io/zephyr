package io.sunshower.gyre;

public class QuadraticProbingCompactHashMap<K, V> extends CompactHashMap<K, V> {

//  protected int probe(int hash, int attempt) {
//    int len = table.length;
//    return hash * (attempt * attempt) % len;
//  }
}
