package io.zephyr.examples;

public class AvailableLanguageTranslationService {

  //    private List<TranslationService> services = new ArrayList<>();
  //
  //
  //    @Override
  //    public void translate(String toTranslate) {
  //        List<TranslationService> translators = services;
  //        for (TranslationService translator : translators) {
  //            translator.translate(toTranslate);
  //        }
  //    }
  //
  //    @Override
  //    public void onEvent(EventType type, Event event) {
  //
  //        if (type == CapabilityEvent.Type.CapabilityAdded) {
  //            services.add(event.getTarget());
  //        }
  //
  //        if (type == CapabilityEvent.Type.CapabilityRemoved) {
  //            services.remove(event.getTarget());
  //        }
  //
  //    }
}
